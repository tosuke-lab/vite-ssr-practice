import ReactDOM from "react-dom";
import { App } from "./App";

let reactRoot: ReactDOM.Root;

const rootEl = document.getElementById("app");
if (rootEl != null) {
  // reactRoot = ReactDOM.hydrateRoot(rootEl, <App />);
}
